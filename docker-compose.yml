# vpnUP — Protocol-Agnostic Base Compose File
# The protocol service is added via docker-compose.override.yml at deploy time.
# Use: docker compose -f docker-compose.yml -f docker-compose.override.yml up -d

version: "3.8"

networks:
  vpn-net:
    driver: bridge
    name: vpnup-net
